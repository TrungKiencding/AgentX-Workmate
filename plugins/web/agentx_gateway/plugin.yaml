name: web-agentx-gateway
version: 1.0.0
description: "AgentX AI Gateway web search — a Perplexity search preset called through the signed-in AgentX account's own gateway key. Nothing to set up: available once the account's key carries a web search grant from the second brain."
author: AstralX Technology
kind: backend
provides_web_providers:
  - agentx-gateway
